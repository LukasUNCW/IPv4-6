# IPv4/6
 
These programs translate the associated IP address format into its binary representation. 

In order for the IPv6 file to run, the user must run the following command, "pip install ipaddress",

inside their IDE terminal to install the 'ipaddress' module.
